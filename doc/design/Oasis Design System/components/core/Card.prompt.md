Base content surface used for mobile list rows, policy summaries, and dashboard tiles — the mobile stand-in for the desktop products' dense tables.

```jsx
<Card><PolicyRow /></Card>
```

16px radius, 1px `--border-default`, `--shadow-sm`. Set `padded={false}` when the child manages its own padding (e.g. a list of rows).
