Screen header with optional back chevron, title, and right-side action slot. `dark` variant uses the navy surface (login/splash contexts).

```jsx
<TopBar title="Claim Details" onBack={goBack} />
```
