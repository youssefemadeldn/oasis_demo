/// Route **names** — used as each `GoRoute`'s `name:`, never as a raw path.
class AppRoutes {
  const AppRoutes._();

  static const String splash = 'splash';
  static const String login = 'login';
  static const String home = 'home';
  static const String unknown = 'unknown';
}
